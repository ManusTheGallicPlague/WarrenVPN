# WarrenVPN system group. Installed to /usr/lib/sysusers.d/.
#
# The daemon runs as root; this group owns shared/managed configuration files and
# the root-owned shadow copies (which are group-readable so the daemon and tooling
# can read them without world access). No login user is created.
g warrenvpn -
